/**
 * snapgauge — contract tests for MCP servers (SPEC docs/SPEC.md).
 *
 * M0 surface: version constant only. The snapshot model, canonicalizer,
 * `record`/`diff` over the fixture transport, and the first six diff rules
 * land at M1 (SPEC §10).
 */
export { SNAPGAUGE_VERSION } from "./core/version.js";
